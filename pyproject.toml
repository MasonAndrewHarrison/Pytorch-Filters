[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pytorch-filters"
version = "0.1.0"
dependencies = [
    "torch",
    "numpy",
]

[tool.setuptools.packages.find]
include = ["pytorch_filters*"]